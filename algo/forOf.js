// Arrays
const myArray = tableau = [ 'element 1', 2, true, 'lala']

for (let element of myArray){
    console.log(element) // retourne valeurs
}

// Ne fonctionne pas sur les objets
// uniquement sur collection d'objets itérables (objet 'normal' pas iterable ? À voir)